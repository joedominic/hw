# ResumeElite — direct dependencies only (transitive deps resolved by pip).
#
# Install (two steps — upstream conflict: jobspy pins markdownify<0.14, browser-use needs >=1.2):
#   pip install -r requirements.txt
#   pip install --no-deps -r requirements-jobspy.txt
#
# torch: not listed — Docker installs CPU wheels (see Dockerfile). Local dev: pip install torch
# After install, run: playwright install chromium

# --- Web stack ---
Django==5.2.11
django-environ>=0.13.0
django-hijack>=3.7.4
django-ninja>=1.5.3
gunicorn>=22.0.0

# --- Background tasks (Huey + Redis) ---
huey>=2.6.0
redis>=5.0.0
croniter>=3.0.0

# --- Security / validation ---
cryptography>=46.0.5
pydantic>=2.12.0,<3

# --- LLM orchestration (LangGraph resume optimizer + vetting) ---
langchain-core>=0.3.83,<0.4
langgraph>=1.0.1,<2
langchain-openai>=0.3.35,<0.4
langchain-groq>=0.3.8,<0.4
langchain-google-genai>=2.1.12,<3
# Anthropic: langchain-anthropic pulls the `anthropic` SDK; also used directly in llm_services.py
langchain-anthropic>=0.3.22,<0.4
openai>=2.16.0,<3
groq>=0.30.0,<1.0.0
tiktoken>=0.12.0
ollama>=0.3.0

# --- Embeddings & job ranking ---
numpy>=1.26.3,<2
sentence-transformers>=2.2.0
rank-bm25>=0.2.0

# --- Job search (python-jobspy installed separately — see requirements-jobspy.txt) ---
beautifulsoup4>=4.12.2,<5.0.0
tls-client>=1.0.1,<2.0.0
pandas>=2.1.0,<3.0.0
regex>=2024.4.28,<2025.0.0
requests>=2.32.5
markdownify>=1.2.0,<2.0.0

# --- Document I/O (PDF parse, resume export) ---
pdfplumber>=0.11.9
reportlab>=4.2.5
python-docx>=1.1.2
pillow>=12.1.1

# --- Apply agent (ATS adapters + browser-use fallback) ---
# browser-use 0.13.x pins groq==1.0.0 which conflicts with langchain-groq (<1.0.0).
browser-use>=0.11.7,<0.12.0
playwright>=1.49.0
psutil>=5.9.0
